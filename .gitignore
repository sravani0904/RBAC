# ------------------------------
# 🌐 Root-level (for full project)
# ------------------------------

# Node dependencies (both backend and frontend)
node_modules/

# Environment files (contain sensitive info like MongoDB URI, JWT secret)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs (generated automatically)
dist/
build/

# OS generated files
.DS_Store
Thumbs.db

# Editor / IDE settings
.vscode/
.idea/
*.swp

# Coverage / testing
coverage/
.nyc_output/

# Temporary or cache files
*.cache
.cache/
tmp/
temp/
*.pid
*.seed
*.pid.lock

# Optional: if you store uploaded files locally
uploads/
public/uploads/

# SSL or private keys (never commit these)
*.pem
*.key
*.crt

# ------------------------------
# ⚛️ React Frontend (client/)
# ------------------------------
client/node_modules/
client/.env
client/.env.local
client/.env.development.local
client/.env.production.local
client/.env.test.local

# React build folder
client/build/
client/dist/

# Tailwind cache (if used)
client/.next/
client/.turbo/
client/.parcel-cache/

# Redux DevTools (if logs stored locally)
redux-logs.json

# ------------------------------
# 🧩 Express Backend (server/)
# ------------------------------
server/node_modules/
server/.env
server/.env.local
server/.env.development.local
server/.env.test.local
server/.env.production.local

# Build folder (if using Babel/TypeScript)
server/dist/
server/build/
server/logs/
server/tmp/

# ------------------------------
# 🐋 Optional (if using Docker)
# ------------------------------
docker-compose.override.yml
.docker/
